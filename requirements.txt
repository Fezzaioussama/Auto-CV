Flask==3.0.0
Flask-CORS==4.0.0
PyLaTeX==1.4.2
python-dotenv==1.0.0
requests==2.31.0
nltk==3.8.1
scikit-learn==1.3.2
numpy==1.24.3
pypdf==3.17.0
matplotlib==3.8.2
seaborn==0.13.2
# Smart CV Generator dependencies
pandas==2.1.1
pillow==10.1.0

# --- Accounts, persistence and security (public web app) ---
Flask-SQLAlchemy==3.1.1   # ORM + DB session (SQLite dev, Postgres via DATABASE_URL)
Flask-Login==0.6.3        # session-based authentication
Flask-Limiter==3.8.0      # rate limiting on API / LLM endpoints
Flask-WTF==1.2.1          # CSRF protection for browser form/JSON requests
email-validator==2.1.1    # validate registration email addresses

# --- PDF / DOCX CV ingestion ---
pdfplumber==0.11.4        # extract text from uploaded PDF CVs
python-docx==1.1.2        # extract text from uploaded Word CVs

# Optional: LLM integration
# ollama
# openai
